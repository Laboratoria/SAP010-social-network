import { log } from '../src/lib/authUser';

describe('login', () => {
  it('loginCorreto', () => {
    //mockar a função getAppAuth e signInWithEmailAndPassword
    expect(then).toBe('function');
  });
});
//testes assincronos (await e try catch)